{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Start app",
            "type": "shell",
            "command": "pwsh ${workspaceFolder}/start.ps1",
            "presentation": {
                "reveal": "silent"
            },
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": []
        }
    ]
}